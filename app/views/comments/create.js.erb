var comment = {
  'body': '<%= @comment.body %>',
  'commenter': '<%= @comment.user.name %> ',
  'when': '<%= time_ago_in_words(@comment.created_at) %>'
};

Comments.addComment(comment);

console.log(comment); //Output the created comment in the console

document.getElementById('comment_body').value = ''; // Clear up the textarea after comment is rendered
